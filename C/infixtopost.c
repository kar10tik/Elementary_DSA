#include <stdio.h>
#include <stdlib.h>

int A[10];

int infix_to_post(char exprn)
{
    int i;
    for (i=0; i<(length(exprn)); i++)
    {
        ;
    }
    return 0;
}

int main()
{
    char exp;
    puts("Convert infix to postfix");
    puts("Enter an expression");
    gets(exp);
    infix_to_post(exp);
    return 0;
}